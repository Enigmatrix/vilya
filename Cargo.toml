[package]
name = "vilya"
version = "0.1.0"
edition = "2021"

[features]
overwrite = [ "bindgen" ]
bindgen = [ "dep:bindgen" ]

[dependencies]
sc = "0.2.7"

[build-dependencies]
bindgen = { version = "0.69", optional = true }
